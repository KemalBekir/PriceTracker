import React from "react";

type Props = {
  itemsPerPage: number;
  totalItems: number;
  paginate: (pageNumber: number) => void;
};

const Pagination = (props: Props) => {
  return <div className="flex gap-3 flex-wrap p-6 py-12">
    
  </div>
};

export default Pagination;
